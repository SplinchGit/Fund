version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build        # this runs "vite build" under the hood
  artifacts:
    baseDirectory: dist       # Vite’s default output folder
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
